# Advanced: Noncommutative Examples - Schema for MathHook Knowledge Base
# Generated: 2025-12-15 18:28:00 UTC
# Source: mathhook/docs/src/advanced/noncommutative-examples.md

topic: "advanced.noncommutative_examples"
title: "Noncommutative Algebra Examples"
description: |
  Comprehensive examples of noncommutative algebra in MathHook covering
  quantum mechanics, matrix algebra, and quaternion rotations.

code_refs:
  rust: "mathhook_examples::noncommutative"
  python: "mathhook.examples.noncommutative"
  nodejs: "mathhook.examples.noncommutative"

examples:
  - title: "Quantum Commutator"
    explanation: "Position-momentum canonical commutation relation [x,p] = iℏ"
    code:
      rust: |
        let x = symbol!(x; operator);
        let p = symbol!(p; operator);

        // Commutator: [x, p] = xp - px
        let xp = expr!(x * p);
        let px = expr!(p * x);
        let commutator = expr!(xp - px);

        // LaTeX output
        let latex = commutator.to_latex(None).unwrap();
        // Output: \hat{x}\hat{p} - \hat{p}\hat{x}
      python: |
        from sympy.physics.quantum import Operator, Commutator

        x = Operator('x')
        p = Operator('p')

        # Commutator
        comm = Commutator(x, p)
        # Result: I*hbar (in quantum mechanics)
      nodejs: |
        const x = symbol('x', {type: 'operator'});
        const p = symbol('p', {type: 'operator'});

        // Commutator
        const xp = x.mul(p);
        const px = p.mul(x);
        const comm = xp.sub(px);

  - title: "Matrix Equation Left Division"
    explanation: "Solve A*X = B using left division X = A^(-1)*B"
    code:
      rust: |
        let solver = MatrixEquationSolver::new();
        let A = symbol!(A; matrix);
        let X = symbol!(X; matrix);
        let B = symbol!(B; matrix);

        // Equation: A*X - B = 0
        let equation = expr!((A * X) - B);

        let result = solver.solve(&equation, &X);
        // Returns: X = A^(-1)*B (left division)
      python: |
        from sympy import symbols, MatrixSymbol, solve, Eq

        A = MatrixSymbol('A', n, n)
        X = MatrixSymbol('X', n, n)
        B = MatrixSymbol('B', n, n)

        # Solve A*X = B
        equation = Eq(A*X, B)
        solution = solve(equation, X)
        # Returns: X = A^(-1)*B
      nodejs: |
        const A = symbol('A', {type: 'matrix'});
        const X = symbol('X', {type: 'matrix'});
        const B = symbol('B', {type: 'matrix'});

        const equation = A.mul(X).sub(B);
        const solution = solve(equation, X);
        // Returns: X = A.inv().mul(B)

  - title: "Quaternion Multiplication"
    explanation: "Noncommutative quaternion basis multiplication i*j = k, j*i = -k"
    code:
      rust: |
        let i = symbol!(i; quaternion);
        let j = symbol!(j; quaternion);

        // i*j = k
        let ij = expr!(i * j);

        // j*i = -k (different!)
        let ji = expr!(j * i);

        // Order matters
        assert_ne!(ij.to_string(), ji.to_string());
      python: |
        from sympy.algebras.quaternion import Quaternion

        i = Quaternion(0, 1, 0, 0)
        j = Quaternion(0, 0, 1, 0)
        k = Quaternion(0, 0, 0, 1)

        # Verify: i*j = k
        assert i * j == k

        # Verify: j*i = -k (noncommutative!)
        assert j * i == -k
      nodejs: |
        const i = symbol('i', {type: 'quaternion'});
        const j = symbol('j', {type: 'quaternion'});

        const ij = i.mul(j);  // k
        const ji = j.mul(i);  // -k

related_topics:
  - "advanced.noncommutative_algebra"
  - "advanced.noncommutative_api_reference"
  - "advanced.matrices"
  - "physics.quantum_mechanics"

metadata:
  schema_version: "1.0"
  source_file: "advanced/noncommutative-examples.md"
  timestamp: "2025-12-15T18:28:00Z"
