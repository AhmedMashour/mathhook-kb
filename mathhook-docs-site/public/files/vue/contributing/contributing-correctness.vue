<template>
  <div class="doc-page">
    <header class="doc-header">
      <h1>Mathematical Correctness</h1>
      <p class="description">Mathematical correctness is Priority Level 1. Nothing else matters if the math is wrong.
Covers validation, edge cases, symbol assumptions, numerical precision, and domain restrictions.
</p>
    </header>

    

    <section class="examples">
      <h2>Interactive Examples</h2>
      
    </section>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const activeTab = ref('python')

// SEO metadata
const metaDescription = "Mathematical correctness is Priority Level 1. Nothing else matters if the math is wrong.
Covers validation, edge cases, symbol assumptions, numerical precision, and domain restrictions.
"
const keywords = []

// Define page metadata
definePageMeta({
  title: 'Mathematical Correctness',
  description: metaDescription,
  keywords: keywords.join(', '),
  
})
</script>

<style scoped>
.doc-page {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem;
}

.doc-header h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.description {
  font-size: 1.2rem;
  color: #666;
}

.math-definition {
  background: #f5f5f5;
  padding: 1.5rem;
  border-radius: 8px;
  margin: 2rem 0;
}

.example-card {
  border: 1px solid #ddd;
  border-radius: 8px;
  padding: 1.5rem;
  margin-bottom: 2rem;
}

.code-tabs {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1rem;
}

.code-tabs button {
  padding: 0.5rem 1rem;
  border: none;
  background: #eee;
  cursor: pointer;
  border-radius: 4px;
}

.code-tabs button:hover {
  background: #ddd;
}

.code-block {
  background: #1e1e1e;
  color: #d4d4d4;
  padding: 1rem;
  border-radius: 4px;
  overflow-x: auto;
}

.output {
  margin-top: 1rem;
  padding: 1rem;
  background: #f9f9f9;
  border-left: 4px solid #42b883;
}
</style>
