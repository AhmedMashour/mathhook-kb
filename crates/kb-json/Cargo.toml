[package]
name = "kb-json"
version = "0.1.0"
edition = "2021"
description = "JSON schema data generator for MathHook Knowledge Base"

[dependencies]
kb-core = { path = "../kb-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
