[package]
name = "kb-colab"
version = "0.1.0"
edition = "2021"
description = "Google Colab notebook generator with GitHub integration"

[dependencies]
kb-core = { path = "../kb-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
